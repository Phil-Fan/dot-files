# 别名配置模块
# 文件: ~/.zsh_config/aliases.zsh

{{ if eq .chezmoi.os "darwin" }}
# macOS 应用快捷方式
alias typora="open -a Typora"
{{ end }}

# 常用别名
alias ll="ls -lh"
alias la="ls -lha"
alias ..="cd .."
alias ...="cd ../.."
