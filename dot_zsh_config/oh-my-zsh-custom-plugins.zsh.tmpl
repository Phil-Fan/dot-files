# Oh My Zsh 自定义插件管理
# 文件: ~/.zsh_config/oh-my-zsh-custom-plugins.zsh

# 自定义插件目录
export ZSH_CUSTOM="${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}"

# 确保自定义插件目录存在
mkdir -p "$ZSH_CUSTOM/plugins"
mkdir -p "$ZSH_CUSTOM/themes"
